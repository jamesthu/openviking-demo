# 规则文件

把用户维护的业务检索规则和回答规则放在这个目录里。

推荐每个规则文件都采用下面这个结构：

```md
# Rule: rule-name

## Scope
描述这条规则适用于哪些问题。

## Retrieval
描述推荐的检索顺序和工具选择。

## Answer
描述最终回答必须包含哪些内容。

## Updates
后续追加的修改记录按时间写在这里。
```

可以在对话里使用 `rule_append` 工具自动创建新规则文件，或者给已有规则继续追加内容。
